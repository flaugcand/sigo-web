export function setMostraConsulta(state, value) {
    state.mostraConsulta = value
}

export function setCurrentMenu(state, menu) {
    state.currentMenu = menu
}

export function setCurrentView(state, view) {
    state.currentView = view
}